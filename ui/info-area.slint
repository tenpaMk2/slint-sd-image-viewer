import {
    VerticalBox,
    ScrollView,
    GroupBox,
    TextEdit,
} from "std-widgets.slint";
import { Table } from "table.slint";
import { ViewState } from "view-state.slint";

export component InfoArea inherits ScrollView {
    min-width: 200px;
    VerticalBox {
        alignment: start;

        GroupBox {
            title: @tr("Basic Info");
            content-padding: 1px;

            Table {
                data: [
                    { key: @tr("Filename"), value: "aaa-afewfajewiaofjew-fewafeawfewa-hoge.png" },
                    { key: @tr("Size"), value: "1.2 MB" },
                    { key: @tr("Resolution"), value: "1156 x 864" }
                ];
            }
        }

        GroupBox {
            title: @tr("XMP");
            content-padding: 1px;

            Table {
                data: [
                    { key: "Rating", value: 0 <= ViewState.current-rating ? ViewState.current-rating : "None" }
                ];
            }
        }

        GroupBox {
            title: @tr("Status🚧");
            content-padding: 1px;

            Table {
                data: [{ key: "Auto-Reload", value: ViewState.auto-reload-active ? "Active" : "Inactive" }];
            }
        }

        GroupBox {
            title: @tr("Tags");
            content-padding: 1px;

            VerticalLayout {
                TextEdit {
                    height: 8rem;
                    wrap: word-wrap;
                    read-only: true;
                    text: "1girl, solo, looking at viewer, brown hair, green eyes, blush, smile, indoors, hair ornament, hairclip";
                }
            }
        }

        GroupBox {
            title: @tr("Errors🚧");
            content-padding: 1px;

            TextEdit {
                height: 4rem;
                wrap: word-wrap;
                read-only: true;
                text: ViewState.error-message;
            }
        }
    }
}
